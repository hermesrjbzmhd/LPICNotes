این حس کاملاً طبیعیه. اکثر دوره‌های آموزشی می‌آیند سینتکس‌های تکراری مثل `echo "Hello World"`، یک حلقه `for i in {1..5}` و چند تا متغیر ساده را آموزش می‌دهند؛ ولی وقتی پای کارهای واقعی سیستمی (مثل بکاپ، مدیریت فایل، دسترسی‌ها و تعامل با سیستم‌عامل) وسط می‌آید، آدم حس می‌کند هیچی بلد نیست!

برای اینکه بدانی دقیقاً باید دنبال چه چیزهایی بروی و چطور ذهنت را دسته‌بندی کنی، **جعبه‌ابزار Bash برای یک ادمین سیستم** به ۶ مفهوم کلیدی زیر خلاصه می‌شود:

---

### ۱. آرگومان‌های ورودی و مقادیر پیش‌فرض (Positional Parameters)
خیلی از اسکریپت‌ها قرار نیست با کاربر تعامل متنی داشته باشند، بلکه از خط فرمان ورودی می‌گیرند (مثل `cp file1 file2`).
* **چیزهایی که باید یاد بگیری:**
  * `$1`, `$2`, `$3`: آرگومان اول، دوم و...
  * `$0`: نام خود اسکریپت.
  * `$#`: تعداد کل آرگومان‌های ورودی.
  * تکنیک طلایی مقدار پیش‌فرض: `${2:-default_value}` (یعنی اگر آرگومان دوم خالی بود، از این مقدار استفاده کن).

---

### ۲. شرط‌ها و آزمون‌ها (Test Operators)
در بش، دستور شرطی `if` با دستور `test` یا کروشه `[ ]` کار می‌کند. این آزمون‌ها ۳ دسته اصلی دارند:
* **تست فایل و فولدر:**
  * `-f path`: آیا این یک فایل واقعی است؟
  * `-d path`: آیا این یک پوشه است؟
  * `-e path`: آیا اصلاً چنین فایلی/پوشه‌ای وجود دارد؟
* **تست متن و رشته:**
  * `-z "$str"`: آیا رشته خالی است؟ (خیلی پرکاربرد برای چک کردن وارد شدن آرگومان‌ها).
  * `"$a" = "$b"`: آیا دو رشته برابرند؟
* **تست اعداد:**
  * `-eq` (برابر)، `-ne` (نابرابر)، `-gt` (بزرگتر)، `-lt` (کوچکتر).

---

### ۳. تفاوت حیاتی «اسکریپت مستقل» و «تابع (Function)»
این یکی از مهم‌ترین مفاهیم آزمون‌های LPIC و مصاحبه‌هاست:
* وقتی یک اسکریپت را با `./script.sh` اجرا می‌کنی، سیستم یک **زیرپوسته (Subshell)** می‌سازد. هر کاری آنجا بکنی (مثلاً دستور `cd` بزنی)، به محض تمام شدن اسکریپت از بین می‌رود و ترمینال اصلی تکان نمی‌خورد!
* اما **تابع (Function)** داخل همان شلِ جاری اجرا می‌شود و می‌تواند مسیر جاری ترمینال یا متغیرهای محیطی نشستِ فعال تو را تغییر دهد.

---

### ۴. گرفتن ورودی از کاربر (`read`)
وقتی می‌خواهی در حین اجرای کد از کاربر داده بگیری:
* `read -p "Prompt: " var`: نمایش یک پیام و ذخیره در متغیر.
* `read -s`: حالت مخفی (Silent)؛ کلیدهایی که تایپ می‌شوند در صفحه چاپ نمی‌شوند (مخصوص رمز عبور).

---

### ۵. دریافت خروجی دستورات (Command Substitution)
چطور خروجی یک دستور لینوکسی را داخل یک متغیر ذخیره کنیم؟
* فرمت مدرن: `VAR=$(command)`
* مثلاً برای گرفتن تاریخ جاری و فرمت‌بندی آن: `TODAY=$(date +%Y-%m-%d)`
* تفکیک مسیر و نام فایل با دو دستور پرکاربرد:
  * `dirname "/path/to/file.txt"` ⬅️ به تو `/path/to` می‌دهد.
  * `basename "/path/to/file.txt"` ⬅️ به تو `file.txt` می‌دهد.

---

### ۶. کد خروج (Exit Code) و کنترل خطا
در لینوکس، هر دستوری که درست اجرا شود مقدار `0` و اگر با خطا متوقف شود عددی غیر از صفر (مثلاً ۱ یا ۲) برمی‌گرداند.
* متغیر `$?`: کد خروج آخرین دستور را نشان می‌دهد.
* `exit 0` (پایان موفق) در برابر `exit 1` (پایان با خطا).
* عملگر `&&` (اگر اولی درست بود دومی را اجرا کن) و `||` (اگر اولی خطا داد دومی را اجرا کن).
* سایلنت کردن خروجی‌ها و خطاها: `>/dev/null 2>&1` (هدایت خروجی به سیاه‌چاله برای اینکه در صفحه چاپ نشود).

---

### گام اول ما برای تمرین:
بیا از **مفهوم اول و دوم (آرگومان‌ها + شرط ساده)** شروع کنیم. 

یک سناریوی فرضی:  
فرض کن می‌خواهیم اسکریپتی بنویسیم به اسم `check_dir.sh` که یک ورودی از کاربر بگیرد (نام یک پوشه):
1. اگر ورودی به آن داده نشده بود، پیام خطا بدهد که «لطفاً مسیر پوشه را وارد کنید» و با کد خطا خارج شود.
2. اگر ورودی داده شده بود، چک کند آیا واقعاً چنین پوشه‌ای وجود دارد یا نه؟
3. اگر وجود داشت بگوید «پوشه یافت شد»، و اگر نبود بگوید «پوشه وجود ندارد».

به نظرت برای نوشتن این سناریوی کوچک، از چه دستوراتی باید استفاده کنیم؟ چقدر از منطقش رو می‌تونی حدس بزنی؟ (هر چقدر بلدی بگو، حتی ناقص).
