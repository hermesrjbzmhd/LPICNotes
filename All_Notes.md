## بخش ۱: پایه‌ها، ساختار فایل و اجرای اسکریپت

### ۱. خط شی‌بنگ (Shebang: `#!/bin/bash`)
* **چیستی و عملکرد:** خط اول فایل که به هسته لینوکس اعلام می‌کند چه مفسری (Interpreter) باید فایل را اجرا کند.
* **تفاوت با پایتون:** اگر بنویسید `#!/usr/bin/python3`، با مفسر پایتون اجرا می‌شود.
* **نکته فنی:** ترجیحاً بدون فاصله نوشته شود: `#!/bin/bash`. اگر نوشته نشود، سیستم از پوسته جاری ترمینال استفاده می‌کند که در سیستم‌های مختلف رفتار یکسان ندارد.

### ۲. پسوند فایل‌ها (`.bash` یا `.sh` یا بدون پسوند)
* **قانون لینوکس:** لینوکس وابسته به پسوند نیست. با مجوز اجرا (`chmod +x file`) و شی‌بنگ، فایل بدون پسوند هم اجرا می‌شود.
* **چرا پسوند می‌گذاریم؟**
  1. ادیتورها (مثل VS Code) رنگ‌بندی کد و اکستنشن‌های کمکی را فعال می‌کنند.
  2. الزامات نام‌گذاری تمرین‌ها یا تست‌های خودکار (مثلاً نام الزامی: `backup_file.bash`).

### ۳. اجرای خط‌به‌خط و دیباگ اسکریپت
* **سوئیچ `-x` (Xtrace):** قبل از اجرای هر خط، دستور را همراه با متغیرهای جایگزین‌شده با علامت `+` چاپ می‌کند.
  ```bash
  bash -x script.sh
  ```
* **دیباگ یک بخش خاص داخل کد:**
  ```bash
  set -x   # شروع رصد خط‌به‌خط
  mkdir -p "$target_dir"
  set +x   # پایان رصد و بازگشت به حالت عادی
  ```
* **توقف خط‌به‌خط با کلید Enter (Interactive Step):**
  ```bash
  trap 'read -p "Press Enter for next line..."' DEBUG
  ```

---

## بخش ۲: معماری متغیرها، رشته‌ها و قواعد نام‌گذاری

### ۱. قواعد و شیوه استاندارد نام‌گذاری
* **متغیرهای معمولی اسکریپت:** حروف کوچک با آندرلاین (`snake_case`):
  ```bash
  source_file="$1"
  backup_dir="./backup"
  ```
* **ثابت‌ها و مقادیر غیرقابل‌تغییر:** حروف بزرگ با `readonly`:
  ```bash
  readonly MAX_RETRIES=5
  ```
* **داخل توابع:** همیشه با پیشوند `local`:
  ```bash
  local user_name="$1"
  ```
* **خطر بازنویسی متغیرهای سیستمی:** نام‌های `PATH`، `USER`، `HOME`، `UID` و `IFS` رزرو سیستم هستند. اگر بنویسید `PATH="/my/dir"`، لینوکس دیگر حتی دستورات پایه مثل `ls` را هم پیدا نخواهد کرد!

### ۲. تعریف متغیر خالی و تفاوت با متغیر تعریف‌نشده
* **تعریف متغیر خالی:**
  ```bash
  target_file=""   # یا: target_file=
  ```
* **تفاوت فنی:** متغیر خالی مقدار طول صفر دارد (`[ -z "$target_file" ]` برابر True است). اما اگر در ابتدای اسکریپت `set -u` بگذارید، به متغیر تعریف‌نشده خطای توقف می‌دهد، ولی به متغیر خالی خطا نمی‌دهد.

### ۳. دام بزرگ: ترتیب تعریف متغیرها (Bash خط‌به‌خط است!)
* متغیرها در ثانیه خوانده شدن مقداردهی می‌شوند و پیوند زنده ندارند.
* **کد دارای باگ:**
  ```bash
  dest_dir=""
  final_path="${dest_dir}/file.txt"  # مقدار می‌شود "/file.txt" و تا آخر ثابت می‌ماند!
  dest_dir="/tmp"                    # تغییر این خط تأثیری روی final_path ندارد
  ```
* **قانون چیدمان درست:**
  1. ثابت‌ها و ابزارهای مستقل بالا (مانند `date` و `basename`).
  2. اعتبارسنجی ورودی‌ها و شرط‌ها وسط.
  3. متغیرهای ترکیبی و وابسته در پایین (بعد از قطعی شدن تک‌تک اجزا).

### ۴. چسباندن رشته‌ها و جلوگیری از تداخل متغیرها
* کافی است متغیرها را داخل `""` کنار هم بگذارید:
  ```bash
  final_name="${prefix}_${file_name}.bak"
  ```
* **چرا آکولاد `{}`؟** اگر بنویسید `$prefix_file`، سیستم دنبال متغیری به اسم `prefix_file` می‌گردد. با `${prefix}_file` مرز متغیر مشخص می‌شود.

### ۵. قانون طلایی کوتیشن `""` برای متغیرها
همیشه دور متغیرها کوتیشن بگذارید تا از باگ فاصله (Word Splitting) جلوگیری شود:
```bash
file="my log.txt"
cat "$file"   # درست
cat $file     # غلط: تلاش برای باز کردن دو فایل مجزا به نام‌های 'my' و 'log.txt'
```

### ۶. مقدار پیش‌فرض هوشمند (`:-`)
* **مقدار پیش‌فرض متنی:**
  ```bash
  file_pref=${2:-report} # اگر آرگومان دوم خالی بود، report بنشیند
  ```
* **مقدار پیش‌فرض از یک متغیر دیگر:**
  ```bash
  user_name="$1"
  group_name="${2:-$user_name}" # اگر گروه وارد نشد، برابر با خود نام کاربر شود
  ```

### ۷. کاراکترهای کنترلی (Escape Sequences)
| کاراکتر | عملکرد |
| :---: | :--- |
| `\n` | رفتن به خط بعدی (Newline) |
| `\t` | ایجاد فاصله تب افقی (Tab) |
| `\\` | چاپ خود علامت بک‌اسلش |
| `\"` | چاپ گیومه داخل متن کوتیشن‌دار |

* **نحوه استفاده صحیح در Bash:**
  ```bash
  echo -e "Line1\nLine2"           # با سوئیچ e-
  printf "Name:\t%s\n" "Ali"       # با دستور استاندارد printf
  msg=$'Line1\nLine2'              # ذخیره درون متغیر با ANSI-C quoting
  ```

---

## بخش ۳: ناوبری، مسیرها و بررسی فایل‌ها و پوشه‌ها

### ۱. تشخیص مسیر مطلق در برابر نسبی
* **مسیر مطلق (Absolute):** همیشه با اسلش (`/`) شروع می‌شود؛ مستقل از پوشه جاری است (`/var/log/syslog`).
* **مسیر نسبی (Relative):** هرگز با `/` شروع نمی‌شود؛ وابسته به محل جاری ترمینال است (`./file.txt` یا `my_vault/report.bak` یا `../back`).

### ۲. علائم اعلان ترمینال (`~`، `$` و `#`)
* **`~` (مکان):** مخفف دایرکتوری خانگی کاربر جاری (`/home/username`).
* **`$` (دسترسی):** کاربر عادی با اختیارات محدود.
* **`#` (دسترسی):** کاربر ریشه (`root`) با اختیارات تام.
* **خروج از کاربر root:** دستور `exit` یا `logout` یا شورتکات `Ctrl + D`.

### ۳. استخراج آدرس، نام فایل و تبدیل مسیرها
| عملیات | دستور / کد | مثال خروجی برای `/var/log/syslog` |
| :--- | :--- | :--- |
| نام فایل بدون مسیر | `basename "$path"` | `syslog` |
| مسیر پوشه بدون نام فایل | `dirname "$path"` | `/var/log` |
| تبدیل به مسیر مطلق سیستم | `realpath "$path"` | `/var/log/syslog` |
| تبدیل مسیر کامل به مسیر نسبی | `realpath --relative-to=. "$path"` | `syslog` (بسته به دایرکتوری جاری) |
| استخراج مطمئن مسیر خود اسکریپت | `realpath "${BASH_SOURCE[0]}"` | آدرس قطعی فایل اسکریپت روی دیسک |

### ۴. مشاهده آدرس کامل فایل‌های دایرکتوری جاری
```bash
realpath *                           # ساده‌ترین راه
ls -d "$PWD"/*                       # با متغیر محیطی PWD
find "$PWD" -maxdepth 1 -type f     # نمایش فقط فایل‌ها بدون پوشه‌ها
```

### ۵. آزمون‌ها و شرط‌های فایل و رشته (`[[ ... ]]`)
* بین براکت‌ها حتماً فاصله بگذارید: `[[ -f "$file" ]]`
* **سوئیچ‌های پرکاربرد:**
  * `-z "$str"`: رشته خالی است.
  * `-n "$str"`: رشته پر است.
  * `-f "$path"`: فایل معمولی وجود دارد.
  * `-d "$path"`: پوشه وجود دارد.
  * `!`: نقیض شرط (Not).

### ۶. ساخت پوشه با `mkdir -p` (سوئیچ Parents & Peace)
1. ساخت پوشه‌های تو در تو: `mkdir -p a/b/c` (اگر والدها نباشند، می‌سازد).
2. عدم توقف و خطا ندادن در صورت وجود پوشه از قبل.

### ۷. تست وجود فایل و ساخت آن در صورت عدم وجود
* **روش شرطی کامل:**
  ```bash
  if [[ ! -f "$file" ]]; then
      touch "$file"
  fi
  ```
* **روش تک‌خطی با عملگر `||`:**
  ```bash
  [[ -f "$file" ]] || touch "$file"
  ```

---

## بخش ۴: ابزارها، ورودی‌ها و کار با محتوای فایل‌ها

### ۱. برچسب زمانی با ابزار `date`
فرمت‌بندی با علامت `+`:
```bash
today=$(date +%Y-%m-%d)           # 2026-09-24
timestamp=$(date +%Y%m%d_%H%M%S)  # 20260924_143550
```

### ۲. دریافت ورودی مخفی (رمز عبور) با `read -s`
* `-p`: متن راهنما.
* `-s`: حالت Silent (عدم نمایش کاراکترها هنگام تایپ).
* **نکته الزامی:** پس از `read -s` حتماً یک `echo ""` بگذارید تا خط بعدی تمیز چاپ شود:
  ```bash
  read -s -p "Enter password: " pass
  echo ""
  ```

### ۳. کپی، انتقال و الحاق محتوای فایل‌ها
* **جایگزینی کامل (Overwrite):**
  ```bash
  cat source.txt > dest.txt
  ```
* **الحاق به انتهای فایل (Append):**
  ```bash
  cat source.txt >> dest.txt
  echo "$secret_key" >> dest.txt
  printf '%s\n' "$secret_key" >> dest.txt   # روش ضدگلوله بدون دستکاری کاراکتر
  ```
* **انتقال/کات کردن کامل فایل:**
  ```bash
  mv source.txt dest.txt
  ```
* **انتقال محتوا و خالی کردن سریع فایل مبدأ:**
  ```bash
  cat source.txt > dest.txt && > source.txt
  ```

### ۴. کدهای خروج و سایلنت کردن پردازش‌ها
* **خروج موفق:** `exit 0`
* **خروج با خطا:** `exit 1` یا `exit 2`
* **هدایت به سیاه‌چاله (`/dev/null`):**
  ```bash
  command > /dev/null 2>&1   # هم خروجی عادی و هم خطاها بی‌صدا می‌شوند
  ```

---

## بخش ۵: توابع شل (Function) در برابر اسکریپت

### تفاوت اساسی (Subshell vs Current Shell)
* **اسکریپت مستقل (`./script.sh`):** در زیرپوسته مجزا (Subshell) اجرا می‌شود. دستور `cd` درون آن، مسیر ترمینال اصلی را تغییر نمی‌دهد.
* **تابع (Function):** در پوسته جاری اجرا می‌شود و می‌تواند دایرکتوری یا محیط نشست فعال را تغییر دهد.

### ساختار تابع استاندارد (POSIX) و پارامترها
پرانتزها همیشه خالی هستند؛ آرگومان‌ها با `$1`, `$2` دریافت می‌شوند:
```bash
mkcd() {
    local target_dir="$1"
    if [[ -z "$target_dir" ]]; then
        echo "Usage: mkcd <dir>" >&2
        return 1
    fi
    mkdir -p "$target_dir" && cd "$target_dir"
}
```

---

## بخش ۶: ساختارهای کنترلی (if, for, while, case)

### ۱. ساختار `if / elif / else`
```bash
if [[ $score -ge 90 ]]; then
    echo "عالی"
elif [[ $score -ge 50 ]]; then
    echo "قبول"
else
    echo "مردود"
fi
```

### ۲. حلقه `for` روی فایل‌ها (بدون استفاده اشتباه از `ls`)
```bash
for log_file in /var/log/*.log; do
    echo "Processing: $log_file"
done
```

### ۳. حلقه `while` برای خواندن خط‌به‌خط فایل
```bash
while IFS= read -r line; do
    echo "Line: $line"
done < users.txt
```

### ۴. ساختار `case` (منوی انتخاب)
```bash
case "$action" in
    start)  systemctl start my_service ;;
    stop)   systemctl stop my_service ;;
    *)      echo "Invalid action" ;;
esac
```

---

## بخش ۷: ابزارها و اکستنشن‌های VS Code

### ۱. شورتکات‌های پرکاربرد
* **کامنت / آن‌کامنت کردن خطوط:** `Ctrl + /` (ویندوز/لینوکس) | `Cmd + /` (مک)
* **بلاک کامنت ادیتور:** `Shift + Alt + A`
* **اجرای متن انتخاب‌شده در ترمینال:** بخش مورد نظر را هایلایت کنید و **`Shift + Enter`** را بزنید.

### ۲. کامنت چندخطی در سینتکس داخلی Bash
ترفند Heredoc با دستور پوچ (`:`):
```bash
: << 'EOF'
این خطوط
اصلاً اجرا
نمی‌شوند.
EOF
```

### ۳. اکستنشن‌های ضروری
1. **ShellCheck:** لینتر و تحلیل‌گر استاتیک. خطاهای امنیتی، اشتباهات در کوتیشن‌گذاری و باگ‌های شل را قبل از اجرا مشخص می‌کند (نصب پکیج اصلی در دبیان: `sudo apt install shellcheck`).
2. **shell-format:** مرتب‌ساز تورفتگی‌ها و تمیزکننده کدهای Bash بر اساس استانداردهای تمیزنویسی (با شورتکات `Ctrl + Shift + I` یا فعال‌سازی `Format on Save`).
3. **Code Runner:** دکمه اجرای تک‌کلیک اسکریپت با `Ctrl + Alt + N`.

---

## بخش ۸: چرخه حرفه‌ای گیت و گیت‌لب (گنج)

### ۱. آماده‌سازی و اتصال مخزن به گنج
```bash
git init
git branch -M main
echo "# Project" > README.md
git add .
git commit -m "chore: initial commit"
git remote add origin <URL_گنج>
git push -u origin main
```

### ۲. چرخه ۳ مرحله‌ای استاندارد برای ثبت هر تغییر
```bash
git status                                   # ۱. بررسی تغییرات
git add problem-1/backup_file.bash           # ۲. استیج کردن تغییرات
git commit -m "feat(backup): add script"     # ۳. ثبت کامیت با پیام استاندارد
git push                                     # ۴. ارسال به مخزن مرکزی
```

### ۳. خارج کردن فایل از حالت استیج (Unstage)
* **روش مدرن و ایمن:**
  ```bash
  git restore --staged filename.txt
  git restore --staged .
  ```
* **روش کلاسیک:**
  ```bash
  git reset HEAD filename.txt
  ```
* ⚠️ **هشدار:** دستور `git restore filename.txt` بدون `--staged`، تمام تغییرات ذخیره‌نشده فایل را برای همیشه پاک می‌کند.

---

## ❓ پرسش و پاسخ‌های تحلیلی و پرتکرار شما (Q&A)

#### س ۱: چرا دستورات لینوکس مثل `ls` و `cd` را می‌توان مستقیم در کد بش نوشت؟
**ج:** چون Bash اصالتاً زبان چسباننده دستورات شل است. هر خطی که می‌نویسید عیناً یک دستور سیستمی است که شل فراخوانی می‌کند.

#### س ۲: چرا دستور `git branch` بلافاصله بعد از `git init` هیچی نشان نمی‌دهد؟
**ج:** در گیت شاخه یک برچسب متصل به یک کامیت است. تا وقتی اولین کامیت ثبت نشود، شاخه در وضعیت «متولدنشده» (Unborn Branch) است و چیزی برای نشان دادن وجود ندارد.

#### س ۳: آیا برای هر مسأله آزمون باید برنچ جدا بسازم؟
**ج:** خیر؛ چون طبق آیین‌نامه آزمون شرکت، ارسال Merge Request ممنوع است (`❌`). باید تمامی پوشه‌ها روی شاخه اصلی قرار داشته باشند و تفکیک فقط با پوشه‌بندی و کامیت‌های مجزا انجام شود.

#### س ۴: تفاوت کروشه تکی `[ ]` و کروشه دوتایی `[[ ]]` چیست؟
**ج:** کروشه تکی همان دستور سنتی `test` استاندارد POSIX است که به فاصله حساس بوده و در صورت خالی بودن متغیرها بدون کوتیشن خطا می‌دهد. کروشه دوتایی کلمه کلیدی داخلی و مدرن Bash است که از عملگرهای منطقی `&&` و `||` و تطبیق الگو بدون شکست متغیرها پشتیبانی می‌کند.

#### س ۵: چطور آدرس پوشه یک فایل را بدون نام فایل به دست آوریم؟
**ج:** با دستور `dirname "$path"`. اگر آدرس کامل و بدون خطای مسیر نسبی نیاز باشد:
```bash
dirname "$(realpath "$path")"
```