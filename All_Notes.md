## بخش ۱: راهکار سریع و کاملاً حلال برای سوالات تشریحی

اینکه بعضی مباحث در دوره‌ها نبوده یا در سرچ‌های فارسی/انگلیسی پراکنده است کاملاً طبیعی است؛ چون این سوالات برگرفته از سرفصل‌های واقعی آزمون بین‌المللی **LPIC-1** و سناریوهای محیط کار هستند.

### فرمول پیشنهادی برای یادگیری سریع و بدون شبهه:
برای اینکه هم سرعتت بالا باشد، هم هیچ دروغ یا فریبی در کار نباشد و هم علمت واقعی شود، از **«روش پرسشِ مفهومِ خام»** استفاده کن:

1. **صورت سوال امتحانت را فراموش کن.**
2. فقط **مفهومِ پشت آن** را مطرح کن.  
   * ❌ *نپرس:* «پاسخ سوال ۶ تشریحی رو بنویس.»
   *  *بپرس:* «مفهوم Swap و مکانیزم Swappiness در لینوکس چیه، چرا هسته ازش استفاده می‌کنه و دستورات تغییرش چیه؟» یا «فلسفه امنیتی استفاده از sudo به جای root چیه؟»
3. من عملکرد آن را در سیستم‌عامل، پشت‌صحنه فایل‌سیستم، و دستوراتش را دقیق برایت باز می‌کنم.
4. تو مفهوم را هضم می‌کنی، صفحه چت را می‌بندی، و در کانفلوئنس **با قلم، درک و کلمات خودت** آن را مستند می‌کنی.

در این حالت:
* کارَت **۱۰۰٪ مشروع و یادگیری خالص** است؛ چون سر کلاس منتور نشسته‌ای و مفهوم را فهمیده‌ای.
* در «تحویل حضوری»، اگر مربی هر سؤالی از دل آن متن بپرسد، چون واقعاً مکانیزمش را درک کرده‌ای، مثل آب خوردن پاسخ می‌دهی.

---

## بخش ۲: تفکیک پوشه‌ها و فایل‌ها برای سوالات عملی (۲ به بعد)

**پاسخ صریح: حتماً و قطعاً باید پوشه و فایل مجزا بسازی و جداگانه کامیت کنی! اصلاً نباید همه را داخل فایل مسأله اول بریزی.**

### ۳ دلیل فنی مهم برای این کار:

1. **الزام صریح سند آزمون:**  
   در جدول قوانین نوشته شده: **`محل ثبت پاسخ: گنج > پروژه تمرینات الیا > پوشه مسأله`**.  
   عبارت «پوشه مسأله» دقیقاً یعنی هر مسأله باید یک دایرکتوری اختصاصی برای خودش داشته باشد.

2. **فلسفه ابزارها:**  
   اسکریپت اول `backup_file.bash` یک ابزار مستقل برای فایل پشتیبان است. قرار دادن تابع ساخت کاربر (`member`) یا تابع جابجایی دایرکتوری (`mkcd`) داخل یک فایل بکاپ، از نظر معماری لینوکس و برنامه‌نویسی کاملاً غلط است (اصل تک‌مسئولیتی یا Single Responsibility).

3. **حرفه‌ای بودن تاریخچه گیت:**  
   ارزیاب وقتی وارد گنج می‌شود، می‌خواهد ببیند شما گام‌به‌گام مسائل را جلو برده‌اید و کامیت زده‌اید.

---

### 📂 ساختار استاندارد مخزن شما در گنج:

روی شاخه اصلی، ساختار پروژه‌ات را این شکلی بچین:

```text
elia-tasks/
├── .gitignore
├── README.md
│
├── problem-1/
│   └── backup_file.bash
│
├── problem-2/
│   └── mkcd.bash
│
├── problem-3/
│   └── member.bash
│
└── problem-4/
    └── proxy_demo.sh
```

---

### چطور توابع مسأله ۲ و ۳ را در فایل بگذاریم؟

در سوال ۲ گفته **«نام تابع: `mkcd`»** و در سوال ۳ گفته **«نام تابع: `member`»**.  
این‌ها قرار نیست یک اسکریپت با دستورات خطی باشند، بلکه یک تابع کامل هستند.

#### برای مسأله ۲ (`problem-2/mkcd.bash`):
فایل را باز می‌کنی و تابع را داخلش می‌نویسی:
```bash
#!/bin/bash

mkcd() {
    # بدنه تابع
    # بررسی ورودی
    # ساخت پوشه با mkdir -p
    # ورود به پوشه با cd
}
```
*ارزیاب برای تست کردن این تابع، فایل شما را با دستور `source problem-2/mkcd.bash` لود می‌کند و سپس دستور `mkcd test_dir` را در ترمینالش صدا می‌زند.*

#### روند کامیت‌گذاری:
* وقتی مسأله ۲ را نوشتی و تست کردی:
  ```bash
  git add problem-2/mkcd.bash
  git commit -m "feat(navigation): implement mkcd shell function"
  git push
  ```
* وقتی مسأله ۳ را نوشتی:
  ```bash
  git add problem-3/member.bash
  git commit -m "feat(user-management): implement member function"
  git push
  ```

---
