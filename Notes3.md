### ۱. آیا برای تعریف تابع حتماً باید کلمه `function` را بنویسیم؟

**خیر، الزامی نیست.** در Bash دو روش برای تعریف تابع وجود دارد:

* **روش اول (استاندارد POSIX - توصیه‌شده):**  
  ```bash
  my_func() {
      echo "سلام"
  }
  ```
* **روش دوم (اختصاصی Bash):**  
  ```bash
  function my_func {
      echo "سلام"
  }
  # یا ترکیب هر دو:
  function my_func() {
      echo "سلام"
  }
  ```

💡 **چرا اکثر برنامه‌نویسان حرفه‌ای روش اول (بدون `function`) را می‌نویسند؟**  
چون روش اول در **همه پوسته‌های یونیکسی** (مثل `sh`، `dash`، `zsh` و `bash`) بدون خطا کار می‌کند (پُرتابل است). اما کلمه `function` فقط در Bash و Zsh شناخته می‌شود و اگر اسکریپت با پوسته پیش‌فرض دبیان (`sh`) اجرا شود، خطا می‌دهد.

---

### ۲. چطور برای تابع پارامتر تعیین کنیم؟

در لینوکس، برخلاف پایتون یا زبان‌های دیگر، **هیچ‌وقت متغیر را داخل پرانتز نمی‌نویسیم!** پرانتزها همیشه خالی هستند: `func()`  
تابع در Bash دقیقاً مثل یک **«اسکریپت کوچک داخل اسکریپت اصلی»** رفتار می‌کند و آرگومان‌هایش را با همان شماره‌ها می‌گیرد (`$1`, `$2`, ...):

```bash
#!/bin/bash

# تعریف تابع با پارامتر
say_hello() {
    # تعریف متغیر به صورت محلی (local) تا فقط داخل همین تابع زنده بماند
    local NAME="$1"
    local ROLE="$2"

    echo "سلام $NAME! شما به عنوان $ROLE وارد شدید."
}

# نحوه صدا زدن تابع با ارسال پارامترها (بدون پرانتز، فقط با فاصله):
say_hello "علی" "مدیر سرور"
say_hello "سارا" "کارشناس شبکه"
```

> ⚠️ **نکته طلایی (`local`):** داخل تابع همیشه پشت متغیرها کلمه `local` را بگذارید؛ در غیر این صورت متغیر شما در کل اسکریپت پخش می‌شود و ممکن است ناخواسته مقدار بقیه متغیرهای برنامه را تغییر دهد.

---

### ۳. خط اول فایل‌ها (`#!/bin/bash`) اسمش چیست و به چه دردی می‌خورد؟

اسم این خط **Shebang (شی‌بَنگ)** یا **Hashbang** است:
* `#` = Hash / Sharp
* `!` = Bang

#### وظیفه شی‌بنگ:
لینوکس از روی پسوند فایل (`.sh` یا `.bash`) تشخیص نمی‌دهد که فایل با چه برنامه‌ای اجرا شود!  
وقتی فایلتان را اجرایی می‌کنید (`chmod +x script.sh`) و دستور `./script.sh` را می‌زنید، هسته لینوکس دقیقاً **اولین خط فایل** را می‌خواند تا ببیند چه مفسری (Interpreter) باید این کدها را اجرا کند.

* اگر بنویسید: `#!/bin/bash` ⬅️ سیستم با پوسته Bash فایل را اجرا می‌کند.
* اگر بنویسید: `#!/usr/bin/python3` ⬅️ سیستم فایل را با پایتون اجرا می‌کند!
* اگر اصلاً ننویسید: سیستم سعی می‌کند با پوسته پیش‌فرض ترمینالِ جاری فایل را اجرا کند که اگر کدهای خاصِ Bash نوشته باشید، ممکن است به خطای عجیب بخورید.

---

### ۴. مگه نباید توی شرط‌ها دوتا علامت بذاریم؟ (`[ ]` در برابر `[[ ]]`)

منظور شما از آکولاد احتمالاً همان **کروشه تکی `[ ]`** در برابر **کروشه دوتایی `[[ ]]`** است (آکولاد `{ }` برای ساخت بلوک کد یا باز کردن متغیرهاست، مثل `${VAR}`).

این دو تفاوت مهمی دارند:

| ویژگی | کروشه تکی `[ ... ]` | کروشه دوتایی `[[ ... ]]` |
| :--- | :--- | :--- |
| **نام فنی** | دستور استاندارد `test` | ساختار مدرن و داخلی Bash (Keyword) |
| **سازگاری** | همه‌جا کار می‌کند (حتی در `sh` قدیمی) | فقط در Bash و Zsh کار می‌کند |
| **نیاز به کوتیشن** | **حتماً** باید متغیر داخل `""` باشد تا در صورت خالی بودن ارور ندهد: `[ "$a" = "ali" ]` | خطایاب بهتری دارد و حتی بدون کوتیشن هم کار می‌کند: `[[ $a == "ali" ]]` |
| **عملگرها** | از `&&` و `||` داخل خودش پشتیبانی نمی‌کند | از `&&` و `||` مستقیماً داخل شرط پشتیبانی می‌کند: `[[ -f $file && -r $file ]]` |
| **رجکس و الگو** | ندارد | از رجکس پیشرفته با عملگر `=~` پشتیبانی می‌کند |

> **قانون ساده:** اگر می‌دانید که اسکریپت قرار است در **Bash** اجرا شود (و شی‌بنگ `#!/bin/bash` دارد)، همیشه از **`[[ ]]`** استفاده کنید چون کار با آن بسیار راحت‌تر، امن‌تر و مدرن‌تر است.

---

### ۵. دستورات پرکاربرد شرطی و حلقه‌ها با مثال‌های کوتاه

#### الف) ساختار `elif` (چند شرطی)
وقتی چند حالت مختلف برای بررسی داریم:
```bash
#!/bin/bash
SCORE=82

if [[ $SCORE -ge 90 ]]; then
    echo "عالی (A)"
elif [[ $SCORE -ge 75 ]]; then
    echo "خوب (B)"
elif [[ $SCORE -ge 50 ]]; then
    echo "قبول (C)"
else
    echo "مردود (F)"
fi
```
*(نکته: `-ge` مخفف Greater or Equal یعنی بزرگتر یا مساوی است).*

---

#### ب) حلقه `for` (تکرار روی لیست یا فایل‌ها)

* **حالت ۱: پیمایش روی یک لیست دلخواه:**
  ```bash
  for item in "پوشه_یک" "پوشه_دو" "پوشه_سه"; do
      echo "در حال بررسی: $item"
  done
  ```

* **حالت ۲: پیمایش روی فایل‌های یک پوشه (بسیار پرکاربرد در ادمینی):**
  ```bash
  # تمام فایل‌های با پسوند log را در مسیر جاری پردازش می‌کند
  for logfile in *.log; do
      echo "نام فایل لاگ: $logfile"
  done
  ```

* **حالت ۳: شمارش عددی (شبیه زبان C):**
  ```bash
  for ((i=1; i<=3; i++)); do
      echo "تکرار شماره: $i"
  done
  ```

---

#### ج) حلقه `while` (تکرار تا برقراری یک شرط)

* **شمارنده ساده:**
  ```bash
  COUNT=1
  while [[ $COUNT -le 3 ]]; do
      echo "شمارش: $COUNT"
      ((COUNT++)) # یکی به متغیر اضافه می‌کند
  done
  ```

* **خواندن خط‌به‌خط یک فایل متنی (یکی از سوالات پرتکرار مصاحبه‌ها):**
  ```bash
  # فایلی به اسم users.txt را سطر به سطر می‌خواند
  while IFS= read -r line; do
      echo "خط خوانده‌شده: $line"
  done < users.txt
  ```

---

#### د) ساختار `case` (جایگزین تمیزتر برای ifهای طولانی)
وقتی ورودی کاربر چند حالت مشخص دارد (مثلاً منوهای انتخاب):
```bash
#!/bin/bash
ACTION="$1"

case "$ACTION" in
    start)
        echo "سرویس روشن شد."
        ;;
    stop)
        echo "سرویس متوقف شد."
        ;;
    status)
        echo "سرویس فعال است."
        ;;
    *)
        echo "دستور نامعتبر است! از start, stop یا status استفاده کنید."
        ;;
esac
```
---

### ۱. ساده‌ترین و مدرن‌ترین روش: دستور `realpath`
دستور `realpath` کارش این است که مسیر نسبی را بگیرد و مسیر کامل (مطلق) را تحویل دهد:
```bash
realpath *
```
* این دستور برای تک‌تک فایل‌ها و پوشه‌های دایرکتوری فعلی، آدرس کامل (شروع از `/`) را چاپ می‌کند.
* اگر فایل‌های مخفی (Hidden که با نقطه شروع می‌شوند) را هم می‌خواهید:
  ```bash
  realpath * .*
  ```

---

### ۲. ترفند کلاسیک با `$PWD` و `ls`
متغیر محیطی `$PWD` همیشه شامل آدرس کامل دایرکتوری جاری شماست. اگر آن را با ستاره ترکیب کنید، خودِ Bash مسیر کامل را می‌سازد:
```bash
ls -d "$PWD"/*
```
* **چرا این کار می‌کنه؟** شل ابتدا `$PWD/*` را به آدرس‌های کامل باز می‌کند و سپس به `ls` تحویل می‌دهد.
* سوئیچ `-d` باعث می‌شود اگر پوشه‌ای وجود داشت، فقط نام خودش را بیاورد و محتویات داخلش را باز نکند.

---

### ۳. روش حرفه‌ای با دستور `find` (برای فیلتر کردن)
اگر در مسیری با فایل‌های زیاد هستید و می‌خواهید مثلاً **فقط فایل‌ها** (نه پوشه‌ها) را ببینید:
```bash
find "$PWD" -maxdepth 1 -mindepth 1 -type f
```
* چون نقطه شروع را `$PWD` دادیم، کل خروجی را با آدرس کامل چاپ می‌کند.
* `-maxdepth 1`: فقط در همین پوشه بگردد و داخل زیرپوشه‌ها نرود.
* `-type f`: فقط فایل‌ها را نشان دهد (اگر `-type d` بگذارید، فقط پوشه‌ها را نشان می‌دهد).

---

### 💡 چطور داخل اسکریپت Bash از این استفاده کنیم؟
اگر داخل اسکریپت می‌خواهید روی آدرس کامل فایل‌ها حلقه بزنید، ساده‌ترین فرمول این است:

```bash
for filepath in "$PWD"/*; do
    echo "مسیر کامل این فایل است: $filepath"
done
```
