این یک تمرین جامع و کاربردی است که دقیقاً برای محک زدن همین مفاهیم طراحی شده است. سناریو کاملاً مستقل و آموزشی است، اما **تک‌تک بلوک‌های ساختمانی** که یک ادمین لینوکس در اسکریپت‌نویسی به آن‌ها نیاز دارد را پوشش می‌دهد:

---

### 🎯 عنوان تمرین: اسکریپت بایگانی لاگ‌های حساس (`secure_archive.sh`)

#### صورت مسئله:
می‌خواهیم اسکریپتی بنویسیم که از یک فایل لاگ یا فایل متنی، یک کپی بایگانی‌شده در پوشه‌ای مشخص ایجاد کند.

#### مراحل و نیازمندی‌های اسکریپت:

1. **بررسی آرگومان اول (فایل مبدأ - اجباری):**
   * بررسی کند آیا آرگومان اول وارد شده است یا خیر؟ اگر خالی بود، پیام خطایی به این شکل بدهد:  
     `Usage: ./secure_archive.sh <file_path> [prefix]`  
     و با کد خطای `1` خارج شود (`exit 1`).
   * بررسی کند آیا فایلی که آدرس داده شده **واقعاً یک فایل معتبر** است یا نه؟ اگر وجود نداشت، بگوید `File not found` و با کد خطای `2` خارج شود.

2. **بررسی آرگومان دوم (پیش‌وند نام فایل - اختیاری):**
   * اگر کاربر آرگومان دوم را وارد کرد، از همان استفاده شود.
   * اگر کاربر آرگومان دوم را وارد **نکرد**، مقدار پیش‌فرض آن به صورت خودکار برابر با کلمه `report` باشد.

3. **دریافت رمز تایید از کاربر (ورودی مخفی):**
   * اسکریپت در خط فرمان متوقف شود و بگوید: `Enter secret key: `
   * حین تایپ کاربر، **هیچ کاراکتری در ترمینال چاپ نشود (حالت سایلنت/مخفی)**.
   * کلید واردشده را در یک متغیر ذخیره کند (فعلاً فقط ذخیره شود تا از دریافت ورودی مخفی مطمئن شویم).

4. **پوشه مقصد و نام‌گذاری با تاریخ:**
   * پوشه مقصدی به نام `my_vault` در نظر بگیرد. اگر این پوشه وجود ندارد، آن را ایجاد کند.
   * با استفاده از ابزار `date`، تاریخ امروز را با فرمت سال-ماه-روز (مثلاً `2026-09-24`) دریافت کند.
   * نام اصلی فایل مبدأ را بدون مسیرش استخراج کند (مثلاً از `/var/log/syslog` فقط `syslog` بماند).
   * نام نهایی فایل در مقصد باید این شکلی باشد:  
     `my_vault/[prefix]_[نام_اصلی]_[تاریخ].bak`

5. **عملیات کپی و کنترل خروجی:**
   * فایل مبدأ را به فایل مقصد کپی کند.
   * در صورت موفقیت، پیام دهد: `Archive created successfully at [مسیر نهایی]` و با کد `0` تمام شود.

---

### 🌟 بخش تکمیلی (آشنایی عملی با Function):
برای اینکه تفاوت تابع و اسکریپت را عملاً حس کنی:
* خارج از فایل بالا، فرض کن می‌خواهی دستوری به نام `govault` بسازی که هر بار در ترمینال تایپ کردی، فوراً ترمینال تو را ببرد داخل پوشه `my_vault` (`cd my_vault`). 
* این را باید به شکل یک **تابع (Function)** تعریف کنی.

---

### 🛠️ نوبت شماست!
سعی کن این سناریو را مرحله‌به‌مرحله روی سیستم خودت بنویسی.  
اصلاً نگران خطا دادن نباش؛ تا جایی که ذهنت یاری می‌کند بنویس و کد یا خطاهایی که می‌گیری را بفرست تا با هم خط‌به‌خط بررسیش کنیم و اشکالات را رفع کنیم.
