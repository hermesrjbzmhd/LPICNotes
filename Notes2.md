
### ۱. آرگومان‌های ورودی و مقدار پیش‌فرض (`$1`, `$2` و `${2:-default}`)

وقتی در لینوکس دستوری مثل `cp file1.txt /tmp/` را می‌زنی، کلمات بعد از دستور، **آرگومان** هستند. داخل اسکریپت، بش به این ورودی‌ها شماره اختصاص می‌دهد:
* `$0`: نام خود فایل اسکریپت
* `$1`: اولین کلمه بعد از نام اسکریپت
* `$2`: دومین کلمه و...

#### 🔹 مثال ساده:
فایلی به نام `test_args.sh` بساز با این محتوا:
```bash
#!/bin/bash

# $1 ورودی اول و $2 ورودی دوم است
USER_NAME="$1"

# این ترفند جالب یعنی: اگر $2 پر بود، خودش رو بذار؛ اگر خالی بود، کلمه "guest" رو بذار
ROLE="${2:-guest}"

echo "User: $USER_NAME"
echo "Role: $ROLE"
```
حالا دو مدل اجرایش کن تا تفاوت را ببینی:
* **حالت اول (بدون ورودی دوم):**  
  `bash test_args.sh ali`  
  *خروجی:*  
  `User: ali`  
  `Role: guest`  (چون ندادی، مقدار پیش‌فرض نشست)

* **حالت دوم (با هر دو ورودی):**  
  `bash test_args.sh ali admin`  
  *خروجی:*  
  `User: ali`  
  `Role: admin`

---

### ۲. شرط‌ها و آزمون‌ها (`if` و کروشه `[ ]`)

یک نکته فوق‌العاده مهم در Bash: **همیشه باید بین کروشه‌ها فاصله (Space) باشد!** (نوشتن `[a=b]` ارور می‌دهد؛ درستش `[ a = b ]` است).

سه تا از پرکاربردترین بررسی‌ها در ادمینی لینوکس:
1. `-z "$VAR"`: مخفف **Zero** است؛ یعنی بررسی می‌کند آیا این متغیر **خالی** است؟
2. `-f "$PATH"`: مخفف **File** است؛ چک می‌کند آیا در این مسیر یک **فایل واقعی** هست؟
3. `-d "$PATH"`: مخفف **Directory** است؛ چک می‌کند آیا این مسیر یک **پوشه** است؟
4. علامت تعجب `!` همه‌چیز را **برعکس (Not)** می‌کند.

#### 🔹 مثال:
```bash
#!/bin/bash
TARGET="$1"

# ۱. آیا ورودی خالی است؟
if [ -z "$TARGET" ]; then
    echo "خطا: شما هیچ ورودی ندادید!"
    exit 1
fi

# ۲. آیا فایلی که وارد شده وجود ندارد؟
if [ ! -f "$TARGET" ]; then
    echo "خطا: فایل '$TARGET' پیدا نشد یا فایل معمولی نیست!"
    exit 1
fi

echo "همه چیز درسته! فایل وجود دارد."
```

---

### ۳. گرفتن خروجی یک دستور داخل متغیر (`Command Substitution`)

گاهی می‌خواهی خروجی یک دستور لینوکسی را در یک متغیر بریزی تا بعداً از آن استفاده کنی. فرمتش این است: `VAR=$(دستور)`

سه ابزار پرکاربرد که با این روش استفاده می‌شوند:

1. **دستور `date`:** گرفتن تاریخ با فرمت دلخواه
   ```bash
   TODAY=$(date +%Y-%m-%d)
   echo "$TODAY"   # خروجی مثلا: 2026-09-24
   ```

2. **دستور `basename`:** بریدن مسیر و نگه‌داشتن فقط نام فایل
   ```bash
   NAME=$(basename "/var/log/nginx/access.log")
   echo "$NAME"   # خروجی فقط می‌شود: access.log
   ```

3. **دستور `dirname`:** بریدن نام فایل و نگه‌داشتن فقط مسیر پوشه
   ```bash
   DIR=$(dirname "/var/log/nginx/access.log")
   echo "$DIR"    # خروجی فقط می‌شود: /var/log/nginx
   ```

---

### ۴. دریافت ورودی مخفی از کاربر (`read -s -p`)

دستور `read` برای وقت‌هایی است که اسکریپت متوقف شود تا کاربر چیزی تایپ کند و اینتر بزند.
* سوئیچ `-p`: یک متن راهنما (Prompt) قبل از تایپ نشان می‌دهد.
* سوئیچ `-s`: حالت مخفی (Silent)؛ یعنی هر چی تایپ کنی روی مانیتور چاپ نمی‌شود (دقیقاً مثل وقتی که رمز لینوکس را می‌زنی).

#### 🔹 مثال:
```bash
#!/bin/bash
# دریافت پسورد بدون نمایش کاراکترها
read -s -p "لطفاً رمز عبور را وارد کنید: " MY_PASS
echo "" # برای رفتن به خط بعدی چون اینتر کاربر چاپ نشده

echo "طول پسوردی که زدید ${#MY_PASS} کاراکتر است."
```

---

### ۵. کدهای خروج (`exit`) و سایلنت کردن خروجی‌ها

در لینوکس، پشت صحنه هر دستوری که درست کار کند عدد `0` (موفقیت) و اگر خراب شود عددی بین `1` تا `255` برمی‌گرداند.
* در اسکریپت اگر به خطایی خوردی باید بنویسی: `exit 1` (تا بقیه برنامه‌ها بفهمند اسکریپت شکست خورد).
* در خط آخر اسکریپت وقتی همه چیز موفق بود می‌نویسند: `exit 0`.

#### سایلنت کردن (ارسال به سیاه‌چاله):
در لینوکس یک مسیر فرضی داریم به نام `/dev/null` که هر متنی داخلش بریزی محو می‌شود.
* `command > /dev/null`: خروجی‌های عادی را محو می‌کند.
* `command 2> /dev/null`: پیام‌های خطا (Error) را محو می‌کند.
* `command > /dev/null 2>&1`: هم خروجی عادی و هم خطاها را کاملاً بی‌صدا می‌کند.

---

### ۶. تفاوت حیاتی: اسکریپت در برابر تابع (Function)

این نکته را ۹۰ درصد کسانی که لینوکس کار می‌کنند در ابتدا اشتباه می‌کنند!

* **اسکریپت (`./script.sh`):**  
  لینوکس برای اجرای اسکریپت، یک ترمینال مخفی بچه (Subshell) باز می‌کند. اسکریپت داخل آن اجرا می‌شود.  
  اگر در اسکریپت بنویسی `cd /tmp`، دایرکتوریِ آن فرزند عوض می‌شود. به محض اتمام اسکریپت، آن فرزند بسته می‌شود و ترمینال خودت تکان نمی‌خورد!

* **تابع (Function):**  
  داخل **همین ترمینال فعلی خودت** اجرا می‌شود. بنابراین اگر داخل یک Function دستور `cd` بزنی، دایرکتوری ترمینال خودت واقعاً عوض می‌شود.

#### 🔹 چطور یک تابع بنویسیم؟
کافی است این چند خط را مستقیم داخل ترمینالت پیست کنی و اینتر بزنی:
```bash
gotmp() {
    echo "دارم می‌رم به پوشه tmp..."
    cd /tmp
}
```
حالا در ترمینال تایپ کن: `gotmp`  
می‌بینی که واقعاً مسیر ترمینالت به `/tmp` تغییر کرد!
